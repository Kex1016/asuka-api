export type SubmissionType = {
    _id: string
    name: string
    image: string
    suggestedBy: string
    messageId: string
    accepted: boolean
    used: boolean
    won: boolean
    addedAt: Date
}